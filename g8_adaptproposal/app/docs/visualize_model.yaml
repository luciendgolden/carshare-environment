description: "This API will be used to to visualize feature importance in bar chart"
tags:
  - name: Visualize the feature importance
produces:
  - image/png
responses:
    "200":
      description: OK
      schema:
        title: Image of feature importance
        type: object
    "500":
      description: Internal error
      schema:
        title: Error processing request
        type: object
        example:
          responseCode: 500
          responseDesc: Error processing request